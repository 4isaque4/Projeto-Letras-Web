# @letras/contracts

Tipos e contratos compartilhados entre os apps do monorepo.

Escopo atual:

- Contratos de autenticacao
- Contratos de dados de dominio
- Contratos de realtime

Adocao incremental:

- O pacote ja existe para centralizar tipagens
- A migracao de imports do `apps/web` e `apps/api` pode ser feita em etapas
